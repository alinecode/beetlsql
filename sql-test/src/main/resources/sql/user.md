select
===

	```sql
    select *  from order_log 
    -- @where(){
        #{use("condition")}
    -- @}
	```

update
===

	```sql
    select *  from order_log 
    -- @where(){
        #{use("condition")}
    -- @}
	```